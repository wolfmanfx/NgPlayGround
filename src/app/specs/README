# Demo different usecases

 Better console reporter

 1. npm install karma-spec-reporter --save-dev
 2. Extend karma config require('karma-spec-reporter')
 3. Extend reporter array with 'spec' entry -- it will override kjhtml!


#1. Testing services



